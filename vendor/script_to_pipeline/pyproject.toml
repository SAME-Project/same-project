[tool.poetry]
name = "script_to_pipeline"
version = "0.1.0"
description = "Tool for converting python scripts into Pachyderm pipelines."
authors = ["Ben Bonenfant <bonenfan5ben@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.7,<4.0"

[tool.poetry.dev-dependencies]
opencv-python = "^4.5.3"
matplotlib = "^3.5"

[tool.poetry.scripts]
script_to_pipeline = 'script_to_pipeline.__main__:main'

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
